# 概述

## 背景

* 云桌面VOI模式需要在物理机上直接运行虚拟机镜像文件中的操作系统
  * 失去了虚拟化技术的隔离与保护，操作系统获得的了整机的完整访问权限
* 为了保护云桌面系统的完整性，避免被上述操作系统中的程序修改，需要一系列安全解决方案
  * 拦截磁盘的IO，非授权程序无法访问
  * 磁盘上存放云桌面数据的文件系统不应被操作系统直接识别，对应用透明

## 需求

* 文件系统应支持EDK2、Windows、Linux等多个操作系统平台
  * 虚拟机镜像文件中的操作系统有可能是Windows
  * 云桌面系统自身有可能以UEFI程序的形式运行
* 文件系统的API采用私有接口
  * 不接入操作系统提供的统一文件系统接口，实现对应用透明
  * 便于配套开发的安全模块对API进行管控
* 文件系统只存储虚拟机镜像文件
  * 面向大文件的存储与IO设计与优化
  * 云桌面系统只在开机时使用，无需过多考虑性能

## 概要设计

* 不支持目录结构，避免复杂的查找逻辑
* 元数据全部加载至内存
  * 避免复杂的盘上数据结构
  * 大幅降低元数据的IO需求
  * 元数据内存数据结构较小
    * 只支持存储一定数量的文件，能满足业务需求即可
    * 以连续的较大磁盘块来管理文件与磁盘的空间分配
* 只需要支持扇区粒度的IO能力
  * 文件将被映射成虚拟磁盘
    * 虚拟磁盘IO粒度为扇区
    * 操作系统为虚拟磁盘中的文件系统提供了缓存能力
  * 自身无需复杂的缓存管理
* 减少维护需求，异常掉电时不会破坏文件系统元数据
  * 创建或更新元数据时，将修改日志写入新的磁盘空间
  * 文件系统加载时，遍历所有日志，在内存中还原全部元数据
  * 日志链过长时，需要删除冗余的日志信息，生成新的日志链
    * 选定一个日志点，将此点之前的日志重放，基于其包含的的元数据创建新的日志，最后链接该点之后的日志
    * 在超级块中记录新的日志链
      * 修改超级块时无法避免覆盖写，因此采用A/B副本切换模式写入
      * 每个超级快副本都有一个版本号，更新日志链时自增
      * 文件系统加载时，使用版本号较新的超级块
* 分层设计，便于适配不同的操作系统
  * 核心层：平台无关，专注处理文件系统业务逻辑
    * 文件元数据操作的API
    * 数据块在文件内偏移与磁盘偏移的映射计算
    * 日志数据管理
  * IO层：平台相关，专注于IO接收与发送
    * 接入操作系统的通用磁盘层，基于文件创建虚拟磁盘，并获得对应IO
    * 调用操作系统的磁盘IO接口，将文件系统处理后的IO交给物理磁盘处理
